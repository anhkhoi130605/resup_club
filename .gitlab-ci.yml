build_frontend:
  script:
    - cd Front-End-ReactJS
    - npm run build
  rules:
    - changes:
        - Front-End-ReactJS/**/* 

build_backend:
  script:
    - cd Back-End-ASP.Net
    - dotnet build
  rules:
    - changes:
        - Back-End-ASP.Net/**/*